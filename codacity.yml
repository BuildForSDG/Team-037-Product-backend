---
engines:
 rubocop:
   exclude_paths:
     - config/engines.yml
 duplication:
   exclude_paths:
     - '**/__tests__/**' 
     - '**/__tests__/auth.test.js'
     - '**/database/**'
 metric:
   exclude_paths:
     - config/engines.yml
 coverage:
   exclude_paths:
     - config/engines.yml
